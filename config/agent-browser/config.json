{
  "provider": "artbird",
  "plugins": [
    {
      "name": "artbird",
      "command": "/bin/sh",
      "args": ["-c", "exec \"$HOME/.local/bin/agentbrowse\" provider"],
      "capabilities": ["browser.provider"]
    }
  ]
}
