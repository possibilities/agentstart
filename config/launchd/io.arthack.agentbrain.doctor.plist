<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- agentstart-installer-owned: io.arthack.agentbrain.doctor.v1 -->
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>io.arthack.agentbrain.doctor</string>
	<!-- Admission acknowledges a submission long before extraction decides
	     whether it succeeded, so a stranded job has no request left to fail and
	     no reader watching the ledger. This is the reader: it runs the same
	     health report an operator would, and posts a notification when the
	     stranded count grows. Reporting is read-only; it never mutates the
	     ledger, and triage stays an explicit operator act. -->
	<key>ProgramArguments</key>
	<array>
		<string>__PROGRAM__</string>
		<string>doctor</string>
		<string>--notify</string>
		<string>--quiet</string>
	</array>
	<key>EnvironmentVariables</key>
	<dict>
		<key>HOME</key>
		<string>__HOME__</string>
		<key>PATH</key>
		<string>__PATH__</string>
	</dict>
	<!-- The only periodic member of the fleet. Ingestion fails on the timescale
	     of a scrape, not a second, and the notifier only speaks when the count
	     grows, so a slow cadence costs nothing and keeps this off the wake path.
	     RunAtLoad catches a backlog that accumulated while logged out. -->
	<key>RunAtLoad</key>
	<true/>
	<key>StartInterval</key>
	<integer>900</integer>
	<key>ProcessType</key>
	<string>Background</string>
	<key>LowPriorityIO</key>
	<true/>
	<key>Umask</key>
	<integer>63</integer>
	<key>StandardOutPath</key>
	<string>__LOG__</string>
	<key>StandardErrorPath</key>
	<string>__LOG__</string>
</dict>
</plist>
