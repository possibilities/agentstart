<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- agentstart-installer-owned: agentsource.notifier.v1 -->
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>agentsource.notifier</string>
	<!-- The receiver's subscriber for people: one grouped macOS notification
	     whenever a project's primary branch flips between green and red. The
	     socket and the verdict state file both derive from HOME, so the
	     subcommand takes no arguments here. -->
	<key>ProgramArguments</key>
	<array>
		<string>__PROGRAM__</string>
		<string>notify-daemon</string>
	</array>
	<!-- PATH must reach terminal-notifier in the Homebrew prefix. -->
	<key>EnvironmentVariables</key>
	<dict>
		<key>HOME</key>
		<string>__HOME__</string>
		<key>PATH</key>
		<string>__PATH__</string>
	</dict>
	<key>RunAtLoad</key>
	<true/>
	<!-- Resident: it reconnects to the receiver itself, so launchd only has
	     to keep the process alive. -->
	<key>KeepAlive</key>
	<true/>
	<!-- Nobody waits on it; a banner a few seconds late is still a banner. -->
	<key>ProcessType</key>
	<string>Background</string>
	<key>Umask</key>
	<integer>63</integer>
	<key>ThrottleInterval</key>
	<integer>10</integer>
	<key>StandardOutPath</key>
	<string>__LOG__</string>
	<key>StandardErrorPath</key>
	<string>__LOG__</string>
</dict>
</plist>
